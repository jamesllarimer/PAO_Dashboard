spring:
  application:
    name: Pao_Dashboard
  datasource:
    url: jdbc:postgresql://localhost:5434/pao
    driver-class-name: org.postgresql.Driver
    username: pao_dev
    password: pao_dev10
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  flyway:
    enabled: true
    locations: classpath:db/migration
server:
  port: 8080
